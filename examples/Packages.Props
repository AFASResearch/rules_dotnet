<?xml version="1.0" encoding="utf-8"?>
<Project>
  <!-- We use https://github.com/Microsoft/MSBuildSdks/tree/master/src/CentralPackageVersions
       to centrally manage the nuget versions -->
  <ItemGroup>
    <!-- <PackageReference Update="System.Data.SqlClient" Version="4.8.2" /> -->

    <!-- These are dependencies of rules_dotnet. Currently we must scaffold them in @nuget. Should be removed in the future -->
    <PackageReference Update="dnlib" Version="3.3.1" />
    <PackageReference Update="Google.Protobuf" Version="3.11.4" />
  </ItemGroup>
</Project>
